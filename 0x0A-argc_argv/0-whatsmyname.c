#include <stdio.h>

/**
 * main - prints its name, followed by new line.
 * @argc: number of command line arguments.
 * @argv: arrays that contains the program command line arguments.
 * Return: 0 - success.
 */
int main void(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
